# Changelog

All notable changes to this plugin will be documented here.

## 1.0.4

- Setup leads with this plugin so MCP is never used without the skills in `skills/`.
- Claude Code can install the same plugin from the repo marketplace.

## 1.0.3

- Logo is SVG (`assets/logo.svg`).

## 1.0.2

- Shipped as `plugins/rentstag` in a Cursor marketplace repo so Add from GitHub can parse the listing.

## 1.0.1

- Logo is the Rentstag symbol (`assets/logo.png`).

## 1.0.0 — initial release

- Added the `rentstag` MCP server pointing at `https://mcp.rentstag.com`.
- Auth is WorkOS sign-in, not an API key.
